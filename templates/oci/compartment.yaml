# Generated by Impacto CLI
# Provider: OCI (Oracle Cloud Infrastructure)
# Resource: Compartment
# API: identity.oci.upbound.io/v1alpha1
# Docs: https://docs.oracle.com/en-us/iaas/Content/Identity/
#
# INSTRUCTIONS:
# 1. Ensure provider-oci-identity is installed on your Crossplane cluster
# 2. This is typically the FIRST resource you create in OCI
# 3. All other resources (VCN, buckets, databases) reference this compartment
# 4. Apply: kubectl apply -f this-file.yaml
#
apiVersion: identity.oci.upbound.io/v1alpha1
kind: Compartment
metadata:
  name: my-compartment  # CHANGE: your compartment name
  annotations:
    crossplane.io/external-name: my-compartment
spec:
  forProvider:
    # REQUIRED: Name for the compartment
    name: my-compartment  # CHANGE: your compartment name

    # REQUIRED: Description
    description: "Compartment for my-project resources"  # CHANGE: describe your compartment

    # OPTIONAL: Parent compartment OCID
    # Leave empty to create in root compartment (tenancy)
    compartmentId: ""  # CHANGE: parent compartment OCID if not root

    # OPTIONAL: Enable deletion (required for kubectl delete to work)
    enableDelete: true  # default: false

    # OPTIONAL: Freeform tags
    freeformTags:
      environment: development  # CHANGE: your environment
      project: my-project       # CHANGE: your project name
      managed-by: crossplane

    # OPTIONAL: Defined tags
    # definedTags:
    #   MyNamespace:
    #     MyKey: MyValue

  providerConfigRef:
    name: default  # CHANGE: your OCI ProviderConfig name (kubectl get providerconfig)

  deletionPolicy: Delete